-- BigQuery dataset bootstrap for the Global Payments Risk & Merchant Intelligence project.
-- Project: global-payments-intelligence
-- Location: EU

CREATE SCHEMA IF NOT EXISTS `global-payments-intelligence.payments_intelligence`
OPTIONS (
  location = 'EU',
  description = 'Synthetic global payments analytical warehouse for portfolio analysis'
);

-- Source tables are loaded from validated Parquet files generated by Python:
--   fact_transactions
--   dim_customer
--   dim_merchant
--   dim_device
--   dim_country
--   dim_date
--
-- The fact table should be partitioned by DATE(transaction_timestamp) and
-- clustered by merchant_id, customer_id and transaction_status when loaded.
